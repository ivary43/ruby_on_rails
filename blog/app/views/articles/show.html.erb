<p>
  <strong>Title:</strong>
  <%= @article.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @article.description %>
</p>

<%= link_to 'Delete', article_path(@article.id), method: :delete, data: {confirm: 'Are you sure'}%>
<%= link_to 'Edit', edit_article_path(@article.id) %>
<%= link_to 'Articles Path', articles_path %>
